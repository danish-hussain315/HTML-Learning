lecture 3
* 6 heading tags

ShortCuts
* to duplicate the HTML element shotcut is (alt + down arrow)
* to add content or other HTML tag (h1>h2) or (p>lorem)
* double cursur (alt + shift + click where ou want other cursor)
(a:) // suggest value
(cntrl + space)  // it will suggest the property values

lecture 3
* 6 heading tags

ShortCuts
* to duplicate the HTML element/otherline shotcut is (alt + ahift +down arrow)
* to add content or other HTML tag (h1>h2) or (p>lorem)
* double cursur (alt + click where ou want other cursor)
(a:) // suggest value
(cntrl + space)  // it will suggest the property values
ul>li*4   // this will add 4 <li></li> to ul

Global Attributes that used in every element of HTML